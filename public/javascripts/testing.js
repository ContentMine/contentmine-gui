// JS Testing environment.

// Socket.io testing.
/*
var socket = io('http://localhost:3000');
console.log("is run");
socket.connect();
socket.on('test', function (data) {
    console.log("HEY!");
});
*/

/*
// Perfect scrollbar testing
$(document).ready(function() {
    //var Ps = require('../javascripts/libraries/angular-perfect-scrollbar/perfect-scrollbar.min');
    var psContainer = document.getElementById('output-panel');
    psContainer.perfectScrollbar({
        wheelSpeed: 2,
        wheelPropagation: true,
        minScrollbarLength: 30
    });
});
*/